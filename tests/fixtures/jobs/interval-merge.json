{
  "schema_version": "1",
  "job_id": "fixture-interval-merge",
  "capability": "interval.merge.v1",
  "inputs": {
    "bed": "../interval-intersect/left.bed"
  },
  "execution": {
    "mode": "local-cpu"
  },
  "parameters": {
    "output": "../../../target/test-results/interval-merge.bed"
  }
}
