# Blog mirror — bio-sdk-reproducibility-benchmark (2026-09-18)

Mirrored copies of the live blog post, kept in-repo alongside the
verification data and hashes the post references (§7.4 of the post).

- source: Linxira-OS/Linxira-OS.github.io `src/posts/`
  (`bio-sdk-reproducibility-benchmark.zh.md` / `.en.md`), snapshot 2026-09-18
- `SHA256SUMS.txt` — hashes of the mirrored markdown files; a mismatch
  against the live site means the post has been edited after this snapshot

Structure (both languages): TL;DR → changelog → 1 background → 2 data →
3 environment → 4 methods → 5 results (5.1-5.5, single authoritative
boundaries list in 5.5) → 6 derivation and independent verification
(6.1 derivation + control experiment, 6.2 SHA256 byte-identity,
6.3 follow-up validation incl. the full three-way sweep) → 7 reproduction
(commands consolidated; 7.4 archive note pointing at `benchmark-results/`)
→ 8 engineering records (integrity-gate lesson, WGCNA CLI contract defect)
→ 9 conclusion → developer's note → versions and declarations (REFORMS
self-assessment: 23 met / 9 N-A / 0 unmet).
