interface:
  display_name: "FASTQ Read Processing"
  short_description: "Trim and deduplicate local FASTQ reads"
  default_prompt: "Use $process-fastq-reads to trim or deduplicate local FASTQ reads and preserve the versioned result."
