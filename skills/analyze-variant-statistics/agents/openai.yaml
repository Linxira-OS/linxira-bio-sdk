interface:
  display_name: "Analyze and Transform VCF"
  short_description: "Summarize, filter, or normalize local VCF data"
  default_prompt: "Use $analyze-variant-statistics to summarize, filter, or reference-normalize this VCF dataset safely."
