interface:
  display_name: "Genome Annotation Analysis"
  short_description: "Analyze, normalize, extract, and profile GFF/GTF features"
  default_prompt: "Use $analyze-genome-annotations to validate this GFF3 or GTF file and run the appropriate local annotation capability."
