interface:
  display_name: "Analyze Sequence Similarity"
  short_description: "Run and inspect local sequence similarity searches"
  default_prompt: "Use $analyze-sequence-similarity to run or inspect a verified local BLAST+, DIAMOND, or HMMER workflow."
