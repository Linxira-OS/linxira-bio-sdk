interface:
  display_name: "Analyze Expression Matrix"
  short_description: "Run local expression QC, normalization, PCA, and clustering"
  default_prompt: "Use $analyze-expression-matrix to validate and run the appropriate local exploratory analysis on this expression matrix."
